params:

files:

paths:
  log: ./runs
  raw_data: ${hydra:runtime.cwd}/pradt/data/raw
  processed_data: ${hydra:runtime.cwd}/pradt/data/processed
  final_data: ${hydra:runtime.cwd}/pradt/data/final

repos:
  repos_dir: ${hydra:runtime.cwd}/repos_name/repos.csv
  repo_name:

features:
  repo_data: repo_data
  commits: commits
  issues_pulls: issues_pulls
  forks: forks
  stargazer: stargazer
  contributors: contributers
  watchers: watchers
